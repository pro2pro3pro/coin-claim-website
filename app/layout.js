export const metadata = {
  title: "Nhận Coin Free",
  description: "Web nhận coin từ bot Discord.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="vi">
      <body className="bg-black text-white">{children}</body>
    </html>
  );
}
